import React from 'react'

const Home = () => {

  // const currentUser =users.find((user)=>user.id===id)
  // if (currentUser) {
    
  // }


  return (

    <div>
    </div>
  )
}

export default Home